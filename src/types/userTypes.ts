// For Define Schema types Typescript.
// 1. for user registration.
export interface userTypes {
    _id: string,
    name: string,
    email: string,
    password: string,
    createdAt: Date;
    updatedAt: Date;
}